diff a/app/src/main/java/com/icmen/codecase/ui/fragment/productDetail/ProductDetailViewModel.kt b/app/src/main/java/com/icmen/codecase/ui/fragment/productDetail/ProductDetailViewModel.kt	(rejected hunks)
@@ -48,16 +48,20 @@ class ProductDetailViewModel : ViewModel() {
                     )
                     basketRef.set(productData)
                         .addOnSuccessListener {
+                            // todo: bunlari strings'e tasi yada constanst class'a tasi ve ordan kullan
                             _basketResponse.value = Resource.Success("Ürün sepete eklendi: ${product.productName}")
                         }
                         .addOnFailureListener { e ->
+                            // todo: bunlari strings'e tasi yada constanst class'a tasi ve ordan kullan
                             _basketResponse.value = Resource.Error("Sepete eklenirken hata oluştu: ${e.message}")
                         }
                 }
             }.addOnFailureListener { e ->
+                // todo: bunlari strings'e tasi yada constanst class'a tasi ve ordan kullan
                 _basketResponse.value = Resource.Error("Sepet kontrol edilirken hata oluştu: ${e.message}")
             }
         } else {
+            // todo: bunlari strings'e tasi yada constanst class'a tasi ve ordan kullan
             _basketResponse.value = Resource.Error("Önce giriş yapmalısınız.")
         }
     }
