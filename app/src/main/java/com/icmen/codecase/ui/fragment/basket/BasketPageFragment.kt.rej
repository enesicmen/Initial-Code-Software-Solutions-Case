diff a/app/src/main/java/com/icmen/codecase/ui/fragment/basket/BasketPageFragment.kt b/app/src/main/java/com/icmen/codecase/ui/fragment/basket/BasketPageFragment.kt	(rejected hunks)
@@ -26,6 +26,13 @@ class BasketPageFragment : BaseFragment<FragmentBasketBinding, BasketPageViewMod
     override fun setViewModelClass() = BasketPageViewModel::class.java
 
     override fun initView(savedInstanceState: Bundle?) {
+        /*
+        todo: apply kullan, adapter ve layout manager'i ayni yerde set et
+        getViewBinding()?.rvBasket?.apply {
+            layoutManager = LinearLayoutManager(requireContext())
+            adapter = basketAdapter
+        }*/
+
         getViewBinding()?.rvBasket?.layoutManager = LinearLayoutManager(requireContext())
         basketAdapter = BasketPageAdapter(basketItems, { }) { position, newQuantity ->
             if (newQuantity == 0) {
@@ -75,11 +82,14 @@ class BasketPageFragment : BaseFragment<FragmentBasketBinding, BasketPageViewMod
             }
         })
     }
+    // todo: arada bosluk birak ve bu methodu sil, kullanmamissin
     private fun setBasketItems(items: List<Product>) {
         basketItems.clear()
         basketItems.addAll(items)
         basketAdapter.notifyDataSetChanged()
     }
+
+    // todo: arada bosluk birak
     private fun showDeleteConfirmationDialog(position: Int) {
         val title = getString(R.string.delete_product)
         val message = getString(R.string.are_you_sure_delete_product)
@@ -89,7 +99,18 @@ class BasketPageFragment : BaseFragment<FragmentBasketBinding, BasketPageViewMod
         }
         dialog.onNoClicked = {}
         dialog.show(requireActivity().supportFragmentManager, "customDialog")
+
+        /*// todo apply kullan, variable (degisken) olusturmana gerek kalmaz
+        CustomDialogWithTwoButtonFragment.newInstance(title, message).apply {
+            onYesClicked = {
+                getViewModel()?.deleteBasketItem(position)
+            }
+            onNoClicked = {}
+            show(requireActivity().supportFragmentManager, "customDialog")
+        }*/
     }
+
+    // todo: arada bosluk birak
     private fun openPaymentPage() {
         getViewBinding()?.btnBuy?.setOnClickListener {
             if (basketItems.isNotEmpty()) {
