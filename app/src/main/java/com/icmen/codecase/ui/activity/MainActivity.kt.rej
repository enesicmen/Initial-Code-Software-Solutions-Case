diff a/app/src/main/java/com/icmen/codecase/ui/activity/MainActivity.kt b/app/src/main/java/com/icmen/codecase/ui/activity/MainActivity.kt	(rejected hunks)
@@ -27,6 +27,17 @@ class MainActivity : AppCompatActivity() {
     private fun onBackPressedDispatcher(){
         onBackPressedDispatcher.addCallback(this, object : OnBackPressedCallback(true) {
             override fun handleOnBackPressed() {
+
+                /*
+                TODO: burada if yerine when kullan
+                when(navController.currentDestination?.id){
+                    R.id.productsPageFragment, R.id.loginPageFragment ->
+                        finishAffinity()
+                    else -> {
+                        navController.popBackStack()
+                        updateBottomNavigationView()
+                    }
+                }*/
                 if (navController.currentDestination?.id == R.id.productsPageFragment) {
                     finishAffinity()
                 }
